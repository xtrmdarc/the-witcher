import 'phaser';

class PlayScene extends Phaser.Scene {
  constructor(config) {
    super(config);

    this.hero;
    this.enemies = [];
  }

  preload() {

  }

  create() {

  }

  preload() {
    
  }

}

export default PlayScene;